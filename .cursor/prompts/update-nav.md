# Update Navigation

I need to update the site navigation on maximized.site.

Change needed: [DESCRIBE CHANGE — e.g., add page, rename item, reorder]

Requirements:
- Update the <nav> block in ALL .html files in the repo root
- Keep the mobile nav hamburger behavior working (nav.js)
- Ensure the active page is highlighted (aria-current="page" + active CSS class)
- Do not break accessibility: <nav aria-label="Primary">, keyboard navigable, focus-visible
- Update components/header.html reference template to match
