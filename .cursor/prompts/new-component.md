# Create New Reusable Component

I need a new reusable UI component for maximized.site.

Component name: [COMPONENT_NAME]
Component purpose: [DESCRIBE]
Where it will be used: [LIST PAGES]

Requirements:
- Write the HTML snippet (to be manually included in pages — no JS includes)
- Write the CSS in assets/css/components.css under a clearly commented block
- Write any required JS in assets/js/ (if stateful behavior needed)
- Follow .cursor/rules/html-standards.mdc and .cursor/rules/accessibility.mdc
- Test: does it work in both light and dark mode?
- Test: is it fully keyboard navigable?
- Test: do all touch targets meet 44×44px minimum?
