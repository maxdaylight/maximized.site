# Update Page Content

I need to update content on maximized.site.

File: [filename.html]
Section: [SECTION NAME OR ID]
Change: [DESCRIBE WHAT TO UPDATE]

Requirements:
- Do not modify <head>, <header>, or <footer> blocks unless specifically asked
- Maintain heading hierarchy (no skipping h1→h3)
- Update meta description and og:description if the page summary changed
- Update sitemap.xml lastmod date for this page
- Maintain all existing CSS classes and JS hooks (data-* attributes, IDs used by JS)
